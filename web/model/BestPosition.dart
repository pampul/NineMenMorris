library BestPosition;
import 'Position.dart';

class BestPosition{
  
  int points;
  Position emplacement;
  Position startEmplacement;
  
  // constructor
  BestPosition([this.points, this.emplacement]);
  
}